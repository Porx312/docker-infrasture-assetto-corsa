# Staging reverse proxy — automatic Let's Encrypt when DNS points directly to VPS (Hostinger).
STAGING_API_HOST=api.projectd.space
STAGING_ADMIN_HOST=admin.projectd.space
AC_DATA_UPSTREAM=127.0.0.1:3000
ACME_EMAIL=admin@projectd.space
